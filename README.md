# bert-serving

bert-as-a-service

The bert-as-service repo is shitty and i have to write it again myself.

```python
cli = Client("localhost")
res = cli.handle(["loli"])
print(type(res))
```

```bash
<class 'numpy.ndarray'>
```
